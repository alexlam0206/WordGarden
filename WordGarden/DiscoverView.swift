
import SwiftUI

struct DiscoverView: View {
    var body: some View {
        NavigationView {
            Text("Discover new words here!")
                .navigationTitle("Discover")
        }
    }
}
